# Generated by LakehousePlumber
# Pipeline: bronze_load
# FlowGroup: partsupp_bronze_dim
# Generated: 2025-07-14T13:45:55.084009

from pyspark.sql import functions as F
import dlt

# Pipeline Configuration
PIPELINE_ID = "partsupp_bronze_dim"
PIPELINE_GROUP = "bronze_load"

# ============================================================================
# SOURCE VIEWS
# ============================================================================


@dlt.view()
def v_partsupp_raw():
    """Load partsupp table from raw schema"""
    df = spark.readStream.table("acmi_edw_dev.edw_raw.partsupp")

    # Add operational metadata columns
    df = df.withColumn("_processing_timestamp", F.current_timestamp())

    return df


# ============================================================================
# TRANSFORMATION VIEWS
# ============================================================================


@dlt.view(comment="SQL transform: partsupp_bronze_cleanse")
def v_partsupp_bronze_cleaned():
    """SQL transform: partsupp_bronze_cleanse"""
    return spark.sql(
        """SELECT
  ps_partkey as part_id,
  ps_suppkey as supplier_id,
  ps_availqty as available_quantity,
  ps_supplycost as supply_cost,
  ps_comment as comment,
  _source_file_path,
  _source_file_size,
  _source_file_modification_time,
  _record_hash,
  _processing_timestamp
FROM stream(v_partsupp_raw)"""
    )


# ============================================================================
# TARGET TABLES
# ============================================================================

# Create the streaming table
dlt.create_streaming_table(
    name="acmi_edw_dev.edw_bronze.partsupp",
    comment="Streaming table: partsupp",
    table_properties={
        "delta.autoOptimize.optimizeWrite": "true",
        "delta.enableChangeDataFeed": "true",
    },
)


# Define append flow(s)
@dlt.append_flow(
    target="acmi_edw_dev.edw_bronze.partsupp",
    name="f_partsupp_bronze",
    comment="Append flow to acmi_edw_dev.edw_bronze.partsupp",
)
def f_partsupp_bronze():
    """Append flow to acmi_edw_dev.edw_bronze.partsupp"""
    # Streaming flow
    df = spark.readStream.table("v_partsupp_bronze_cleaned")

    return df
